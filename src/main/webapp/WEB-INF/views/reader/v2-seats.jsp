<%@ page contentType="text/html;charset=UTF-8" pageEncoding="UTF-8" %><%@ include file="../common/taglib.jsp" %>
<!DOCTYPE html><html><head><meta charset="UTF-8"><title>座位预约</title><link rel="stylesheet" href="${pageContext.request.contextPath}/assets/css/v2.css"></head><body class="v2-body"><div class="v2-layout"><jsp:include page="v2-header.jsp"/><main class="v2-main">
<div class="v2-top"><div><h1>座位预约</h1><p>10×10 座位可视化。灰色可选，绿色为我的预约，红色为他人已预约，黄色为临时锁定。</p></div></div>
<section class="v2-card"><form class="v2-form" method="get"><div><label>楼层</label><select class="v2-select" name="floorId" required><option value="">请选择</option><c:forEach items="${floors}" var="f"><option value="${f.id}" ${floorId==f.id?'selected':''}>${f.floorName}</option></c:forEach></select></div><div><label>日期</label><input class="v2-input" type="date" name="reservationDate" value="${reservationDate}" required></div><div><label>时段</label><select class="v2-select" name="timeSlotId" required><option value="">请选择</option><c:forEach items="${slots}" var="s"><option value="${s.id}" ${timeSlotId==s.id?'selected':''}>${s.startTime}-${s.endTime}</option></c:forEach></select></div><button class="v2-btn primary">查看座位</button></form></section>
<section class="v2-card"><h2>座位图</h2><c:choose><c:when test="${empty floorId or empty reservationDate or empty timeSlotId}"><div class="v2-empty">请选择楼层、日期和时段。</div></c:when><c:otherwise><div class="v2-seat-grid"><c:forEach items="${seats}" var="s"><c:choose><c:when test="${s.seatStatus=='FREE'}"><form method="post" action="${pageContext.request.contextPath}/reader/v2/seats/reserve"><input type="hidden" name="seatId" value="${s.id}"><input type="hidden" name="floorId" value="${floorId}"><input type="hidden" name="reservationDate" value="${reservationDate}"><input type="hidden" name="timeSlotId" value="${timeSlotId}"><button class="v2-seat free" title="${s.seatNo}">${s.seatNo}</button></form></c:when><c:when test="${s.seatStatus=='MINE'}"><button class="v2-seat mine" disabled>${s.seatNo}</button></c:when><c:when test="${s.seatStatus=='LOCKED' or s.seatStatus=='LOCKED_MINE'}"><button class="v2-seat locked" disabled>${s.seatNo}</button></c:when><c:otherwise><button class="v2-seat reserved" disabled>${s.seatNo}</button></c:otherwise></c:choose></c:forEach></div></c:otherwise></c:choose></section>
<section class="v2-card"><h2>我的预约</h2><table class="v2-table"><tr><th>楼层</th><th>座位</th><th>日期</th><th>时段</th><th>操作</th></tr><c:forEach items="${myReservations}" var="r"><tr><td>${r.floorName}</td><td>${r.seatNo}</td><td><fmt:formatDate value="${r.reservationDate}" pattern="yyyy-MM-dd"/></td><td>${r.startTime}-${r.endTime}</td><td><a class="v2-btn danger" href="${pageContext.request.contextPath}/reader/v2/seats/cancel/${r.id}" onclick="return confirm('确认取消自己的预约？')">取消预约</a></td></tr></c:forEach></table></section>
</main></div></body></html>
